# 软件说明：（此说明是为了方便后续代码理解，以防在遇到没有报错的问题时，方便查找原因）
# 1.由于主界面固定的参数样式，所以所有的子界面必须传给主界面的值有如下几个
#     1)task_name，2)sample_list，3)barcode_list，4)work_file，5)task_type
# 2.子界面有且必须有两个信号发送给主界面，通过这两个信号来接收子界面的参数以及调用命令运行程序
#     1)paramSignal，2)startSignal
#     这两个信号均由运行按钮传递给主界面，故须绑定点击事件